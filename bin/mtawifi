#!/usr/bin/bash
iface="wlan0"
sleep=1
while [ 1 ]; do
  #sudo nmcli device wifi list|grep MTA && echo "m t eh"|festival --tts
  sudo nmcli device wifi list|grep -i MTA
  #sudo iwlist $iface scan|grep MTA
  #if [ $? -eq 0 ]; then
  #    $HOME/gits/piloto/scripts/mosq_pub "$(date +%Y%m%d@%T),$(hostname),MTAMOFO"
  #fi
  sleep $sleep
done 
